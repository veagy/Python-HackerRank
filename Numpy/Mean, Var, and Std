import numpy
N, M = map(int, input().split())
mat = numpy.array([input().split() for _ in range(N)], int)
numpy.set_printoptions(legacy='1.13')
print(numpy.mean(mat, axis = 1), numpy.var(mat, axis = 0), numpy.std(mat), sep = '\n')
